services:
  kali-mcp:
    build: .
    container_name: kali-mcp-server
    ports:
      - "127.0.0.1:5050:5050"
    cap_add:
      - NET_RAW
      - NET_ADMIN
    restart: unless-stopped
    # Uncomment below to persist data between restarts
    # volumes:
    #   - ./data:/data
